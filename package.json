{
  "name": "zoom-sdk-config",
  "version": "1.0.0",
  "description": "Serves Zoom SDK config (API key) from Worker secrets — keeps keys out of frontend source code",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
