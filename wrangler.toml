name = "zoom-sdk-config"
main = "src/index.js"
compatibility_date = "2024-01-01"

# Store the API key as a secret — never hardcode it here.
# After deploying, run:
#   npx wrangler secret put ZOOM_API_KEY
